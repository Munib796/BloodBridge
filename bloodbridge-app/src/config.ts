/**
 * ⚠️ THE ONE LINE YOU NEED TO EDIT ⚠️
 *
 * This is the address of your BloodBridge backend (the FastAPI server).
 *
 * - Backend running on your laptop, testing on your phone over the same
 *   WiFi? Use your laptop's LAN IP — NOT "localhost" (your phone can't see
 *   your laptop's localhost). Find your laptop's IP with:
 *     Mac/Linux:  ifconfig | grep "inet "
 *     Windows:    ipconfig
 *   Then set it here, e.g. "http://192.168.1.42:8000".
 *
 * - Backend deployed online (Render, Railway, Fly.io, etc.)? Use that URL,
 *   e.g. "https://bloodbridge-api.onrender.com" (no trailing slash).
 */
export const API_BASE_URL = "http://192.168.100.5:8000";
